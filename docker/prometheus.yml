global:
  scrape_interval: 5s

#  ALERTES - pointe vers le dossier des règles
rule_files:
  - "/etc/prometheus/alerts/*.yml"

scrape_configs:
  - job_name: "backend"
    static_configs:
      - targets: ["backend:3000"]