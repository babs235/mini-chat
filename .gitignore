node_modules/
.env
backend/coverage/
.DS_Store

# Terraform : état local et secrets
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform.lock.hcl
terraform.tfvars.local

# Variables sensibles — les vraies valeurs passent par GitHub Secrets
# terraform.tfvars ne contient QUE des valeurs non-sensibles (aws_region)

MAITRISE_COMPLETE_PROJET.md

# Clés SSH et GCP — Ne JAMAIS versionner
*.pem
*.key
ssh-keys/
gcp-key.json
